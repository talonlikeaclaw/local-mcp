# Local MCP

A containerized setup for running a local [Model Context Protocol (MCP)](https://modelcontextprotocol.io) server with local LLMs.

## Planned Features

- Docker Compose setup for easy local deployment
- MCP server integration for agentic workflows
- Environment-based configuration via `.env`

## Quick Start

```bash
cp .env.example .env
docker compose up
```
